import { render } from "solid-js/web";
import "../global.css";

const Index: Function = () => {
  return <h1>Canvas+ Needs More Permissions</h1>;
};

render(() => <Index />, document.querySelector("#root")!);
