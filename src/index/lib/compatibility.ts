// export const isPathnameCompatible = (fullUrl: string): boolean => {
//   const asUrl = new URL(fullUrl);
//   console.log(asUrl.pathname);

//   return (
//     asUrl.pathname !== "/incompatible_page" &&
//     asUrl.pathname !== "/incompatible_page_2"
//   );
// };
